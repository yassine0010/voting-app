apiVersion: v1
kind: Service
metadata:
  name: vote-service
  namespace: test
  labels:
    app: vote
spec:
  type: ClusterIP
  selector:
    app: vote
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 5000